declare namespace VerticalInfoSchema {
  export interface VerticalInfoType {
    id?: number;
    uuid: string;
    name: string;
    benefits: string;
    description: string;
    image: string;
    image_selected: string;
  }
}

export = VerticalInfoSchema;
